<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="votePage">

	<select id="searchParty" parameterType="VoteVo" resultType="VoteVo">
		<![CDATA[
		SELECT 	P.PARTYNAME, 
				M.MAJORNAME 
		FROM 
				PARTY P, 
				MAJOR M 
		WHERE 
				P.MAJORID = M.MAJORID 
				AND 
				M.MAJORID=123
		]]>
	</select>
	
	<select id="partyDetail" parameterType="VoteVo" resultType="VoteVo">
		<![CDATA[
			SELECT * FROM CANDIDATE 
				WHERE 
				PARTYID = (SELECT PARTYID FROM PARTY WHERE PARTYID = 1234)
			SELECT FILEPATH FROM DATAFILE WHERE FILENO = 1
		]]>
	</select>
	
	<insert id="vote" parameterType="VoteVo">
		<![CDATA[
			INSERT INTO VOTE VALUES (201520867, 2, SYSDATE);
		]]>
	</insert>
	
	

</mapper>